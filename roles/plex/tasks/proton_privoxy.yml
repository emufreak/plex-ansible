---
- name: Ensure Proton Privoxy directories exist
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: plex
    group: plex
    mode: "0755"
  loop:
    - "{{ proton_privoxy_base_dir }}"
    - "{{ proton_privoxy_base_dir }}/config"
